[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "infra-db"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "infra-core",
  "sqlalchemy>=2.0.36",
  "alembic>=1.14.0",
  "asyncpg>=0.30.0",
  "greenlet>=3.1.1",
]

[tool.uv.sources]
infra-core = { workspace = true }
