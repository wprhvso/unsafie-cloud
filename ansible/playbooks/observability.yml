---
- name: deploy observability core on selectel node
  hosts: k3s_servers[1]
  become: true
  roles:
    - observability

- name: deploy vector log shippers across all nodes
  hosts: mesh
  become: true
  roles:
    - vector
